{% extends 'base.html.twig' %}
{% form_theme edit_form 'bootstrap_3_layout.html.twig' %}
{% block body %}
    <h1>Edytuj projekt</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    <div class="btn-group">
        <input class="btn btn-primary" type="submit" value="Edytuj" />
    {{ form_end(edit_form) }}
    <a class="btn btn-info" href="{{ path('projects_index') }}">powrót</a></div>
    <br><br>
    {{ form_start(delete_form) }}
        <input class="btn btn-danger" type="submit" value="Usuń projekt i wszystkie jego raporty">
    {{ form_end(delete_form) }}
{% endblock %}
