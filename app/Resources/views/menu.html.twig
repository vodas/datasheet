<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">WebSiteName</a>
        </div>
        <ul class="nav navbar-nav">
            <li class="active"><a href="#">Home</a></li>
            {% if not app.user %}
                <li><a href="{{ app.request.getBaseURL() }}/login">Login</a></li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <li class="active"><a href="register">Stwórz nowego użytkownika</a></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Pracownik<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                            {% for user in users %}
                                <li><a href="{{ path('employee_employee', {'userid': user.id}) }}">{{ user.username }}</a></li>
                                <li role="separator" class="divider"></li>
                            {% endfor %}
                    </ul>
                </li>
                <li><a href="{{ path('employee_index') }}">Pracownicy</a></li>
            {% endif %}
            {% if  app.user %}
                <p class="navbar-text navbar-right"><a href="{{ app.request.getBaseURL() }}/logout">Logout</a></p>
            {% endif %}
        </ul>
    </div>
</nav>